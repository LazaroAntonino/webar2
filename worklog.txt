# Worklog / Recap

## 2026-01-08
- Ajusté rutas en `src/routes.jsx` para dejar solo la ruta `/` apuntando a `Home`.
- Simplifiqué `Home.jsx` a un placeholder inicial (Hello World) para partir desde cero.

## 2026-01-11
- Diseñé la estructura inicial de la web inmobiliaria premium en `Home.jsx` con doble navbar, modal hero y secciones con switch.
- Componentes creados (cada uno con su CSS):
  - `MainNavbar` / `MainNavbar.css`: navbar principal con logo, ubicaciones y ajustes (iconos `phosphor-react`).
  - `SectionNavbar` / `SectionNavbar.css`: tabs de secciones (Descubrir, Comprar, Vender, Alquilar, Premium) con estado activo.
  - `HeroModal` / `HeroModal.css`: modal/formulario visual de búsqueda (UI estática).
  - `ListingSection` / `ListingSection.css`: panel de contenido contextual según la sección activa.
- Estilos globales en `index.css` (fuente Inter, fondo crema degradado) y layout base en `Home.css`.
- Instalé `phosphor-react` y verifiqué `npm run build` (ok).

## 2026-01-12
- Eliminé toda referencia a `ListingSection` (componente borrado) y añadí un placeholder sencillo para secciones seleccionadas.
- El modal hero ahora solo se muestra cuando no hay sección activa; al seleccionar una sección se muestra la tarjeta "Próximamente".
- Hice clicable el logo/brand del `MainNavbar` para navegar a `/` usando `useNavigate`.
- Al hacer clic en el brand/nav principal, también se resetea `activeSection` para mostrar el modal inicial.
- Restyling general: tipografía global en Arial, paleta corporativa (`#1c2e51` navy, `#fab422` gold), fondos claros.
- Navbar principal y de secciones unificados en estilo minimal; botones con hover en la nueva paleta.
- Hero modal actualizado a tonos navy/gold; tarjetón "Próximamente" alineado a la nueva línea visual.
- Logo del navbar cambiado a `LogoParaFondoBlancoaAR2.png` (en `src/img`).
- Ajuste extra: botones de navbar más minimalistas (sin sombras, fondo transparente) y separación de los dos navbars con una línea fina.
- Navbars ahora comparten altura fija (`--navbar-height`) y se apilan sin hueco; línea fina de separación mantenida.
- Navbar secundario ahora es colapsable en móviles (toggle "Secciones" + menú desplegable suave).
- Rebuild completo de `Home.jsx` a landing premium AR2: hero con overlay azul, buscador flotante, grilla de propiedades destacadas, sección "Por qué elegir", servicios, testimonios, CTA final y footer.
- Tipografía actualizada a Playfair Display (títulos) + Inter (texto); paleta y espaciados acorde brief de lujo sobrio.
- `MainNavbar` rediseñado: menú Propiedades/Nosotros/Servicios/Contacto, CTA "Contáctanos", fondo transparente con transición a sólido en scroll.

## Pendientes / recordatorios
- Navbar doble debe verse idéntico (homogeneizar MainNavbar y SectionNavbar en estilos/altura/fondo).
- Modal hero solo visible cuando no hay sección seleccionada.
- Añadir lógica real de filtros/búsqueda cuando se defina.
- Revisar vulnerabilidades de `npm audit` cuando sea oportuno.
